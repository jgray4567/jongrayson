
## 2026-06-01 Session Notes
- **DataBadger CountyScraper**: Resolved LyteForge DNS issues by routing API calls through OpenClaw's local OpenAI-compatible endpoint (port 18789). `glm-5.1:cloud` proved highly effective at navigating complex portals like Prince William County, whereas smaller local models (`qwen2.5-coder:7b`) caused infinite scraping loops.
- **jongrayson.com Updates**: Rebranded hero copy to "UX Leadership · AI Integration · Design at Scale". When editing `index.html`, stringified HTML inside `<script type="__bundler/template">` blocks must be updated via `sed` or regex due to the bundler structure.