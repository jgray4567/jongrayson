
## 2026-03-20 — Durable updates

- Created Drive folder **SJG** and shared it with `mail4sjg@gmail.com` as writer.
- Created Drive folder **Chat Logs** and stored `Jarvis Chat Backup - 2026-03-19` sheet there.
- GDP Mission Control dashboard built and iterated at `https://jgray4567.github.io/gdp-mission-control/` with repo `jgray4567/gdp-mission-control`.
- GDP dashboard key capabilities now include:
  - Job counter starting at **7804** and auto-increment behavior.
  - Live Jobs table with timeline fields and inline Log Time action.
  - Client Master table with add-client flow, active/inactive/archive status handling, search/sort filters, duplicate detection, tags, last-activity updates, and outstanding balance display.
  - Queue-based actions for `new_job`, `log_time`, `bill_it`, `add_client`, `set_client_status` (and archive via status), plus sync button.
  - Mobile responsiveness improved (dropdown nav, panel ordering, overflow fixes).
- Apps Script write-through integration scaffold created in repo (`apps-script.gs`) with shared token hardening and setup doc in Studio Integration.
- GDP rates policy confirmed: **$100/hour** default unless otherwise stated.
- Studio Integration folder contains Mission Control plan/docs, tooling decision matrix, and client-list work artifacts.
- Client list ingestion/cleanup workflow executed from Estimate/Invoice PDFs; final cleanup v2 performed using only years 2024–2026 for source filtering.
- Giuseppe’s site (`giuseppesva.com`) changes completed:
  - `/preview` workflow active with root placeholder strategy when needed.
  - DNS + GitHub Pages/CNAME troubleshooting completed; site/preview stabilized.
  - Multiple menu typography/content tweaks, mobile spacing/list fixes, and Speakeasy image/logo updates.
  - Facebook links standardized to canonical handle `https://www.facebook.com/giuseppesri` and icon styling adjusted.
- Telegram group-chat connectivity issue resolved by token rotation and config verification; working state confirmed.
- Safety note: bot token was exposed in chat once; user rotated via BotFather and updated config.

