## 2026-04-07
- Built foundational JK Web Team VPS docs in `webteam-vps/`, including `JK-WEBTEAM-VPS-PROFILE.md`, `SITE-REPO-MANIFEST-SPEC.md`, `SITE-REPO-MANIFEST-EXAMPLES.md`, `WEBTEAM-JOB-TYPES-SPEC.md`, and `MANIFEST-VALIDATOR-CHECKLIST.md`.
- Locked product/runtime direction: VPS stays Web Team-only, online-first, static-site-first; Hilda/GDP/local billing behavior should be stripped or sidelined rather than treated as first-class.
- Confirmed source-of-truth policy for sites: GitHub repo is canonical baseline; local folders are not the latest/source-of-truth version.
- Created first real site manifests under `webteam-vps/manifests/sites/` for `a1a-sells` and `giuseppes`.
- Updated `webteam-vps/manifests/sites/giuseppes.json` to use production domain `giuseppesva.com`, alias `www.giuseppesva.com`, and verification URL `https://giuseppesva.com`.
- Began actual JK code cut toward VPS mode in `jerryknows/src/server.js` and `jerryknows/app/index.html` using `JK_PROFILE` / `IS_WEBTEAM_VPS`.
- Exposed profile/runtime state through backend endpoints including `/health` and `/api/system/state` so frontend can detect `webteam-vps` mode.
- Frontend startup in `jerryknows/app/index.html` now skips eager Hilda/billing/drafts/job-counter loading in `webteam-vps` mode and gates billing draft actions there.
- Backend storage behavior in `jerryknows/src/server.js` no longer surfaces Hilda draft cleanup as a first-class VPS concern in `webteam-vps` mode.
- Added manifest-aware backend support in `jerryknows/src/server.js` with helpers/endpoints for listing and fetching site manifests and allowing Web Team jobs to resolve by `siteId` against manifests.
- Telegram on the VPS is working end-to-end for direct chat with Jon via allowlisted user ID `5755593005`; raw Telegram 'allow once' exec approval remains unproven and should not be assumed yet.
- Jon prefers continuing build work asynchronously over Telegram and does not need to stay at the computer except for approvals, secrets, or explicit UI tests.
