
## 2026-03-29 - JerryKnows V3 RAG Backend & API Upgrades
- **API Upgrades:** User hit Google Gemini Free Tier rate limits (50 RPD). Guided user to upgrade Google AI Studio billing to pay-as-you-go and to configure OpenAI API billing. Guided user through `openclaw configure --section model`. OpenClaw configuration now successfully has both `google` and `openai` keys, with `openai/gpt-5.4` as the new system default primary model.
- **Hardware Selection:** Recommended the Samsung 990 PRO 1TB SSD for the Tactical Node build due to its superior thermals inside the passive Satechi USB4 enclosure, power efficiency, and 7,450 MB/s sequential read speed.
- **Agent Roles:** Wrote `4-deploy-coder.sh` to pull and configure `qwen2.5-coder:7b` as the dedicated "JerryCoder" (The Engineer) agent for the Base Station bullpen, using a strict, no-fluff system Modelfile.
- **Local RAG Implementation:**
  - Wrote and executed `5-build-backend.sh` on the Base Station. This stands up a local Node.js Express server on Port 3001.
  - The server uses `multer` for memory buffering, `pdf-parse` to chunk PDFs, and routes chunks to the local `nomic-embed-text` Ollama endpoint for vector embedding.
  - Injected drag-and-drop PDF frontend logic into `jerryknows_tesla_light.html` and uploaded the bundled result as `JerryKnows-Tesla-App-V3-RAG.zip` to Google Drive.
