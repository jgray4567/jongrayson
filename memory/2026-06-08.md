- **Kalshi Safe Compounder Updates**:
  - Reverted `MIN_EDGE` from $0.05 back to $0.03.
  - Reverted `MAX_POSITION_PCT` from 5% back to 10%.
  - Fixed a missing self reference bug where `total_balance` was throwing a `NameError` crash since the logic swap.
  - PM2 process `kalshi-safe-compounder` successfully restarted, cleared the loop crash, and successfully placed maker orders (`KXBTCD-26JUN0805-T63399.99` and `KXHYPE15M-26JUN080445-45`) in live production.
- **Kalshi Safe Compounder Updates**:
  - Reverted `MIN_EDGE` from $0.05 back to $0.03.
  - Reverted `MAX_POSITION_PCT` from 5% back to 10%.
  - Fixed a missing self reference bug where `total_balance` was throwing a `NameError` crash since the logic swap.
  - PM2 process `kalshi-safe-compounder` successfully restarted, cleared the loop crash, and successfully placed maker orders (`KXBTCD-26JUN0805-T63399.99` and `KXHYPE15M-26JUN080445-45`) in live production.

- **Model Lineup Overhaul (Quarterback System v2)**:
  - **Primary daily driver:** `ollama/glm-5.1:cloud` (Ollama Cloud, free tier)
  - **Heavy coding:** `ollama/qwen3-coder:480b-cloud` (Ollama Cloud, free tier, 480B MoE, 262K context)
  - **Local fallback:** `qwen2.5-coder:7b` (4.7GB, Q4_K_M, on Apple Silicon)
  - **All paid models ON HOLD** (brainwave/auto, google/gemini-3.1-pro-preview, openai/gpt-5.4, anthropic/claude-sonnet-4-6, google/gemini-3.5-flash) — Jon explicitly requested no paid tokens from Google, Anthropic, or OpenAI
  - Gateway default changed from `brainwave/auto` → `ollama/glm-5.1:cloud`
  - Gateway restarted to pick up new config
  - Agent acts as "quarterback" — routes coding tasks to qwen3-coder automatically

- **Qwen 2.5 Coder 7B Assessment**:
  - Started 24-hour assessment at 13:25 ET
  - Tracking file: `memory/qwen25-coder-assessment.md`
  - Cron job `bab6e082` will deliver final verdict tomorrow at 1:25 PM ET
  - Decision: don't burn local compute on it; use qwen3-coder cloud instead for heavy tasks

- **Cron Jobs Updated (Ollama-only, no fallbacks to paid models)**:
  - Dream Sequence: `06fbb20b-97be-4785-9b06-316e24ddfbe3` — Mon 9AM ET, `ollama/glm-5.1:cloud` only
  - Karpathy Repo Check: `798107f0-1909-4b1e-9c1b-aef6c9e4adc1` — Daily 8AM ET, `ollama/glm-5.1:cloud` only

- **Dashboard Expandable Cards**:
  - Fixed project cards not expanding — switched from per-element `addEventListener` to event delegation on `document`
  - Overview tab cards now properly bind and expand on click/tap
  - Only one card expanded at a time; tap another collapses previous
  - "tap to expand" hint shown, hidden when expanded
  - Memory file links inside expanded cards open modal view
  - Deployed to https://www.jongrayson.com/coms/

- **Qwen3 Coder Cloud Confirmed Working**:
  - `ollama/qwen3-coder:480b-cloud` responds successfully
  - 262K context window, supports tools
  - No local compute needed — runs entirely on Ollama Cloud