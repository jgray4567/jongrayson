- **CRITICAL - JerryKnows Web Team Facebook Events**: Jon explicitly required that when the Web Team parses/creates new events from Facebook timelines (using the LLM), the output format must be an **exact match** for the existing event structure on the target sites. No deviation allowed. We need to grab the existing JSON schema from the target sites (`giuseppesva-site`, `onehotmess`) and strictly enforce it in the LLM extraction prompt.
- **CRITICAL - JerryKnows Web Team Facebook Events workflow**: The workflow must create/post new events, and automatically purge/remove existing events the day after they occur.
