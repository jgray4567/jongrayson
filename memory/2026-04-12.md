- Built a staged JK Intelligence frontend prototype inside `/Users/jsg/.openclaw/workspace/jerryknows/` because a separate `jongrayson.com` repo does not exist in the workspace yet; long-term destination remains `jongrayson.com/intel` with JerryKnows behind it.
- Build cycle implemented in `jerryknows/app/intelligence/` and `jerryknows/src/server.js`, with supporting task/intel seed files under `jerryknows/data/intelligence/`.
- Build 1 created the first public shell route at `/intelligence/` with top command bar, left task rail, center globe stage, right intel rail, and bottom timeline stub.
- Build 2 introduced the first JerryKnows-side overview payload and wired the shell to fetch/render it.
- Build 3 fixed the repo runtime issue (`pg` missing from `node_modules`) by running `npm install`, then verified `/health`, `/api/intel/overview`, and `/intelligence/` locally.
- Build 4 split the single overview blob into separate Intelligence contracts/endpoints for tasks, alerts, briefing, and timeline.
- Build 5 added a real Cesium-based globe foundation and geocoded marker support.
- Build 6 made `Create Watcher` real with a POST task creation flow and persistence into `data/intelligence/tasks.json`.
- Build 7 added clickable task focus/detail behavior plus task-specific globe fly-to support.
- Build 8 added globe-click AOI capture for new watchers, passing focus coordinates into watcher creation.
- Build 9 added task-scoped alerts and briefing via `/api/intel/tasks/:id/context` backed by `data/intelligence/task-context.json`.
- Build 10 added task-linked temporal state via `/api/intel/tasks/:id/temporal`, including watcher-specific timelines and incidents.
- Build 11 added watcher-specific globe overlays/arcs via `/api/intel/tasks/:id/map` backed by `data/intelligence/task-map.json`.
- Build 12 upgraded watcher creation from point-only focus to polygon/region AOI support, with polygon persistence and draft region drawing on the globe.
- Build 13 added watcher editing with `PATCH /api/intel/tasks/:id` plus incident drill-down modal behavior.
- Build 14 turned the command bar into a real search surface with `/api/intel/search`, returning task and incident hits.
- Build 15 added a task-linked Source Feed panel via `/api/intel/tasks/:id/feed` backed by `data/intelligence/task-feed.json`.
- Build 16 added watcher lifecycle controls: archive, reactivate, and delete, plus visible task state handling in the shell.
- By the end of Build 16, the staged Intelligence shell supports: task creation, editing, search, archive/reactivate/delete lifecycle, task-scoped alerts/briefing/incidents/timeline, task-linked globe overlays, point/polygon AOI capture, and task-linked feed items.
- Recommendation after Build 16 was to continue with active/archived filtering and saved workspace views next.

- Built a staged JK Intelligence frontend prototype inside `/Users/jsg/.openclaw/workspace/jerryknows/` because a separate `jongrayson.com` repo does not exist in the workspace yet; long-term destination remains `jongrayson.com/intel` with JerryKnows behind it.
- Build cycle implemented in `jerryknows/app/intelligence/` and `jerryknows/src/server.js`, with supporting task/intel seed files under `jerryknows/data/intelligence/`.
- Build 1 created the first public shell route at `/intelligence/` with top command bar, left task rail, center globe stage, right intel rail, and bottom timeline stub.
- Build 2 introduced the first JerryKnows-side overview payload and wired the shell to fetch/render it.
- Build 3 fixed the repo runtime issue (`pg` missing from `node_modules`) by running `npm install`, then verified `/health`, `/api/intel/overview`, and `/intelligence/` locally.
- Build 4 split the single overview blob into separate Intelligence contracts/endpoints for tasks, alerts, briefing, and timeline.
- Build 5 added a real Cesium-based globe foundation and geocoded marker support.
- Build 6 made `Create Watcher` real with a POST task creation flow and persistence into `data/intelligence/tasks.json`.
- Build 7 added clickable task focus/detail behavior plus task-specific globe fly-to support.
- Build 8 added globe-click AOI capture for new watchers, passing focus coordinates into watcher creation.
- Build 9 added task-scoped alerts and briefing via `/api/intel/tasks/:id/context` backed by `data/intelligence/task-context.json`.
- Build 10 added task-linked temporal state via `/api/intel/tasks/:id/temporal`, including watcher-specific timelines and incidents.
- Build 11 added watcher-specific globe overlays/arcs via `/api/intel/tasks/:id/map` backed by `data/intelligence/task-map.json`.
- Build 12 upgraded watcher creation from point-only focus to polygon/region AOI support, with polygon persistence and draft region drawing on the globe.
- Build 13 added watcher editing with `PATCH /api/intel/tasks/:id` plus incident drill-down modal behavior.
- Build 14 turned the command bar into a real search surface with `/api/intel/search`, returning task and incident hits.
- Build 15 added a task-linked Source Feed panel via `/api/intel/tasks/:id/feed` backed by `data/intelligence/task-feed.json`.
- Build 16 added watcher lifecycle controls: archive, reactivate, and delete, plus visible task state handling in the shell.
- By the end of Build 16, the staged Intelligence shell supports: task creation, editing, search, archive/reactivate/delete lifecycle, task-scoped alerts/briefing/incidents/timeline, task-linked globe overlays, point/polygon AOI capture, and task-linked feed items.
- Recommendation after Build 16 was to continue with active/archived filtering and saved workspace views next.
- Build 17 added active/all/archived workspace filtering plus persistent saved workspace views, backed by `data/intelligence/views.json` and `GET/POST /api/intel/views`.
- Build 18 added source and severity filtering for the right-rail signal layer so alerts and feed items can be narrowed by source and alert severity.
- Build 19 added persistent saved signal presets, backed by `data/intelligence/signal-presets.json` and `GET/POST /api/intel/signal-presets`, so right-rail source/severity filters can be reused.
- Build 20 added multi-select workspace operations with a batch backend route `POST /api/intel/tasks/batch`, enabling archive, activate, and delete across multiple selected watchers.
- By the end of Build 20, the staged Intelligence shell supports watcher creation/editing/lifecycle, active vs archived filtering, saved workspace views, signal filtering, saved signal presets, and batch watcher actions, making it a more credible analyst workspace rather than a static demo shell.
- Recommendation after Build 20 was to add watcher tags and tag filtering next, because batch actions become much more useful once watchers can be organized by mission, region, topic, or team.
- Build 21 added watcher tags plus workspace tag filtering, with tag-aware task creation/editing, tag-aware search, tag-aware saved workspace views, and seeded tags on the core Red Sea, Taiwan Strait, and Port SAR watchers.
- By the end of Build 21, the staged Intelligence shell supports watcher tags, tag filtering, saved tag-filtered workspace views, signal presets, batch lifecycle actions, and tag persistence on newly created watchers.
- Recommendation after Build 21 was to add bulk tagging next, because once tags exist the practical next step is applying and cleaning them up across many watchers at once.
- Build 22 added bulk tagging for multi-selected watchers via `POST /api/intel/tasks/batch-tags`, supporting batch tag add/remove operations from the left rail.
- Build 23 turned watcher tags into clickable filter chips in both the watcher list and focused watcher card, so tags became direct workspace navigation controls.
- Build 24 added persistent saved tag bundles, backed by `data/intelligence/tag-bundles.json` and `GET/POST /api/intel/tag-bundles`, with starter bundles for Maritime Ops, Air Watch, and SAR Port.
- Build 25 connected saved tag bundles directly to the multi-select workflow, allowing a selected bundle to be applied straight to the selected watchers.
- Build 26 extended batch tag updates with a `replace` mode and added a `Replace Tags` bundle action, so saved bundles can now either augment watcher tags or cleanly overwrite them.
- By the end of Build 26, the staged Intelligence shell supports raw tags, tag filtering, clickable tag navigation, bulk tag add/remove, saved tag bundles, direct bundle apply, and clean bundle-based tag replacement.
- Recommendation after Build 26 was to keep deepening the taxonomy layer, with the next strong candidate being pinned or favorited bundles for faster repeat operations.
- Build 27 added persistent bundle pinning plus pinned-first display so commonly used tag bundles stay quickly reachable.
- Build 28 added bundle rename and delete controls, giving tag bundles a real lifecycle instead of only accumulating.
- Build 29 added bundle health reporting so weak or empty tag bundles can be detected.
- Build 30 added bundle maintenance history, creating a visible record of bundle lifecycle operations and upkeep.
- Build 31 switched bundle analytics to actual recorded usage counts on bundle apply/replace flows, with persistent `usageCount` and `lastUsedAt` on bundle records plus `/api/intel/bundle-analytics`.
- Build 32 added taxonomy health visibility for watcher tags and bundles so the shell can detect thin or weak classification coverage.
- Build 33 added taxonomy groupings, turning raw tags into grouped organizational structures.
- Build 34 added saved taxonomy groups and group-aware workspace filtering.
- Build 35 added group health reporting so weak taxonomy groups can be identified.
- Build 36 added group pinning/favorites so important taxonomy groups stay surfaced.
- Build 37 added group rename/delete lifecycle controls.
- Build 38 added group usage analytics so taxonomy groups can be measured by actual use.
- Build 39 added regroup suggestions so the shell can propose better organizational moves inside the taxonomy layer.
- Build 40 added archive suggestions for watchers, making stale watcher cleanup more operational.
- Build 41 added a maintenance report so watcher/archive hygiene can be reviewed from one surface.
- Build 42 added maintenance history tracking for cleanup and taxonomy operations.
- Build 43 deepened maintenance visibility further so search, taxonomy, and cleanup flows behave more like an operating system than a demo shell.
- Build 44 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 45 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 46 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 47 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 48 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 49 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 50 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 51 expanded saved searches so they store and restore full shell context, including query, task/group/group-state/tag filters, selected watcher, and related shell reload behavior when a saved search is selected.
- Build 52 added saved-search lifecycle controls, specifically rename and delete, backed by `PATCH /api/intel/saved-searches/:id` and `DELETE /api/intel/saved-searches/:id`.
- Build 53 extended saved searches to store and restore signal-layer state too, specifically `signalSourceFilter` and `signalSeverityFilter`.
- Build 54 added persistent saved-search pinning plus pinned quick-access chips and pinned-first ordering in the saved-search dropdown.
- Build 55 added saved-search usage analytics by defaulting saved searches to `usageCount: 0` and `lastUsedAt: null`, tracking usage on selection, and exposing `/api/intel/saved-search-analytics`.
- Build 56 added saved-search health reporting through `/api/intel/saved-search-health`, flagging stale and low-value saved searches in the shell.
- Build 57 added repair actions to saved-search health, using one-click pinning as the first safe repair path for low-value saved searches.
- Build 58 added query analytics through `/api/intel/query-analytics`, exposing ranked search demand from tracked search usage and rendering top query chips in the shell.
- Build 59 added a compact search maintenance summary through `/api/intel/search-maintenance-summary`, summarizing tracked queries, total query uses, saved-search counts, pinned counts, and stale counts.
- Build 60 added saved-search cleanup suggestions plus saved-search `archived` state, including archive suggestions for pinned-but-unused searches and delete suggestions for unpinned unused searches via `/api/intel/saved-search-suggestions`.
- Build 61 added query topic clustering through `/api/intel/query-topics`, grouping tracked query demand into higher-level topics such as `Air Watch` and rendering those topics as a new shell surface.
- Local verification continued through Build 61 using `PORT=8899`, and the staged Intelligence work remained inside `/Users/jsg/.openclaw/workspace/jerryknows/` rather than a separate `jongrayson.com` repo.
- Built a staged JK Intelligence frontend prototype inside `/Users/jsg/.openclaw/workspace/jerryknows/` because a separate `jongrayson.com` repo does not exist in the workspace yet; long-term destination remains `jongrayson.com/intel` with JerryKnows behind it.
- Build cycle implemented in `jerryknows/app/intelligence/` and `jerryknows/src/server.js`, with supporting task/intel seed files under `jerryknows/data/intelligence/`.
- Build 1 created the first public shell route at `/intelligence/` with top command bar, left task rail, center globe stage, right intel rail, and bottom timeline stub.
- Build 2 introduced the first JerryKnows-side overview payload and wired the shell to fetch/render it.
- Build 3 fixed the repo runtime issue (`pg` missing from `node_modules`) by running `npm install`, then verified `/health`, `/api/intel/overview`, and `/intelligence/` locally.
- Build 4 split the single overview blob into separate Intelligence contracts/endpoints for tasks, alerts, briefing, and timeline.
- Build 5 added a real Cesium-based globe foundation and geocoded marker support.
- Build 6 made `Create Watcher` real with a POST task creation flow and persistence into `data/intelligence/tasks.json`.
- Build 7 added clickable task focus/detail behavior plus task-specific globe fly-to support.
- Build 8 added globe-click AOI capture for new watchers, passing focus coordinates into watcher creation.
- Build 9 added task-scoped alerts and briefing via `/api/intel/tasks/:id/context` backed by `data/intelligence/task-context.json`.
- Build 10 added task-linked temporal state via `/api/intel/tasks/:id/temporal`, including watcher-specific timelines and incidents.
- Build 11 added watcher-specific globe overlays/arcs via `/api/intel/tasks/:id/map` backed by `data/intelligence/task-map.json`.
- Build 12 upgraded watcher creation from point-only focus to polygon/region AOI support, with polygon persistence and draft region drawing on the globe.
- Build 13 added watcher editing with `PATCH /api/intel/tasks/:id` plus incident drill-down modal behavior.
- Build 14 turned the command bar into a real search surface with `/api/intel/search`, returning task and incident hits.
- Build 15 added a task-linked Source Feed panel via `/api/intel/tasks/:id/feed` backed by `data/intelligence/task-feed.json`.
- Build 16 added watcher lifecycle controls: archive, reactivate, and delete, plus visible task state handling in the shell.
- By the end of Build 16, the staged Intelligence shell supports: task creation, editing, search, archive/reactivate/delete lifecycle, task-scoped alerts/briefing/incidents/timeline, task-linked globe overlays, point/polygon AOI capture, and task-linked feed items.
- Recommendation after Build 16 was to continue with active/archived filtering and saved workspace views next.

- Built a staged JK Intelligence frontend prototype inside `/Users/jsg/.openclaw/workspace/jerryknows/` because a separate `jongrayson.com` repo does not exist in the workspace yet; long-term destination remains `jongrayson.com/intel` with JerryKnows behind it.
- Build cycle implemented in `jerryknows/app/intelligence/` and `jerryknows/src/server.js`, with supporting task/intel seed files under `jerryknows/data/intelligence/`.
- Build 1 created the first public shell route at `/intelligence/` with top command bar, left task rail, center globe stage, right intel rail, and bottom timeline stub.
- Build 2 introduced the first JerryKnows-side overview payload and wired the shell to fetch/render it.
- Build 3 fixed the repo runtime issue (`pg` missing from `node_modules`) by running `npm install`, then verified `/health`, `/api/intel/overview`, and `/intelligence/` locally.
- Build 4 split the single overview blob into separate Intelligence contracts/endpoints for tasks, alerts, briefing, and timeline.
- Build 5 added a real Cesium-based globe foundation and geocoded marker support.
- Build 6 made `Create Watcher` real with a POST task creation flow and persistence into `data/intelligence/tasks.json`.
- Build 7 added clickable task focus/detail behavior plus task-specific globe fly-to support.
- Build 8 added globe-click AOI capture for new watchers, passing focus coordinates into watcher creation.
- Build 9 added task-scoped alerts and briefing via `/api/intel/tasks/:id/context` backed by `data/intelligence/task-context.json`.
- Build 10 added task-linked temporal state via `/api/intel/tasks/:id/temporal`, including watcher-specific timelines and incidents.
- Build 11 added watcher-specific globe overlays/arcs via `/api/intel/tasks/:id/map` backed by `data/intelligence/task-map.json`.
- Build 12 upgraded watcher creation from point-only focus to polygon/region AOI support, with polygon persistence and draft region drawing on the globe.
- Build 13 added watcher editing with `PATCH /api/intel/tasks/:id` plus incident drill-down modal behavior.
- Build 14 turned the command bar into a real search surface with `/api/intel/search`, returning task and incident hits.
- Build 15 added a task-linked Source Feed panel via `/api/intel/tasks/:id/feed` backed by `data/intelligence/task-feed.json`.
- Build 16 added watcher lifecycle controls: archive, reactivate, and delete, plus visible task state handling in the shell.
- By the end of Build 16, the staged Intelligence shell supports: task creation, editing, search, archive/reactivate/delete lifecycle, task-scoped alerts/briefing/incidents/timeline, task-linked globe overlays, point/polygon AOI capture, and task-linked feed items.
- Recommendation after Build 16 was to continue with active/archived filtering and saved workspace views next.
- Build 17 added active/all/archived workspace filtering plus persistent saved workspace views, backed by `data/intelligence/views.json` and `GET/POST /api/intel/views`.
- Build 18 added source and severity filtering for the right-rail signal layer so alerts and feed items can be narrowed by source and alert severity.
- Build 19 added persistent saved signal presets, backed by `data/intelligence/signal-presets.json` and `GET/POST /api/intel/signal-presets`, so right-rail source/severity filters can be reused.
- Build 20 added multi-select workspace operations with a batch backend route `POST /api/intel/tasks/batch`, enabling archive, activate, and delete across multiple selected watchers.
- By the end of Build 20, the staged Intelligence shell supports watcher creation/editing/lifecycle, active vs archived filtering, saved workspace views, signal filtering, saved signal presets, and batch watcher actions, making it a more credible analyst workspace rather than a static demo shell.
- Recommendation after Build 20 was to add watcher tags and tag filtering next, because batch actions become much more useful once watchers can be organized by mission, region, topic, or team.
- Build 21 added watcher tags plus workspace tag filtering, with tag-aware task creation/editing, tag-aware search, tag-aware saved workspace views, and seeded tags on the core Red Sea, Taiwan Strait, and Port SAR watchers.
- By the end of Build 21, the staged Intelligence shell supports watcher tags, tag filtering, saved tag-filtered workspace views, signal presets, batch lifecycle actions, and tag persistence on newly created watchers.
- Recommendation after Build 21 was to add bulk tagging next, because once tags exist the practical next step is applying and cleaning them up across many watchers at once.
- Build 22 added bulk tagging for multi-selected watchers via `POST /api/intel/tasks/batch-tags`, supporting batch tag add/remove operations from the left rail.
- Build 23 turned watcher tags into clickable filter chips in both the watcher list and focused watcher card, so tags became direct workspace navigation controls.
- Build 24 added persistent saved tag bundles, backed by `data/intelligence/tag-bundles.json` and `GET/POST /api/intel/tag-bundles`, with starter bundles for Maritime Ops, Air Watch, and SAR Port.
- Build 25 connected saved tag bundles directly to the multi-select workflow, allowing a selected bundle to be applied straight to the selected watchers.
- Build 26 extended batch tag updates with a `replace` mode and added a `Replace Tags` bundle action, so saved bundles can now either augment watcher tags or cleanly overwrite them.
- By the end of Build 26, the staged Intelligence shell supports raw tags, tag filtering, clickable tag navigation, bulk tag add/remove, saved tag bundles, direct bundle apply, and clean bundle-based tag replacement.
- Recommendation after Build 26 was to keep deepening the taxonomy layer, with the next strong candidate being pinned or favorited bundles for faster repeat operations.
- Build 27 added persistent bundle pinning plus pinned-first display so commonly used tag bundles stay quickly reachable.
- Build 28 added bundle rename and delete controls, giving tag bundles a real lifecycle instead of only accumulating.
- Build 29 added bundle health reporting so weak or empty tag bundles can be detected.
- Build 30 added bundle maintenance history, creating a visible record of bundle lifecycle operations and upkeep.
- Build 31 switched bundle analytics to actual recorded usage counts on bundle apply/replace flows, with persistent `usageCount` and `lastUsedAt` on bundle records plus `/api/intel/bundle-analytics`.
- Build 32 added taxonomy health visibility for watcher tags and bundles so the shell can detect thin or weak classification coverage.
- Build 33 added taxonomy groupings, turning raw tags into grouped organizational structures.
- Build 34 added saved taxonomy groups and group-aware workspace filtering.
- Build 35 added group health reporting so weak taxonomy groups can be identified.
- Build 36 added group pinning/favorites so important taxonomy groups stay surfaced.
- Build 37 added group rename/delete lifecycle controls.
- Build 38 added group usage analytics so taxonomy groups can be measured by actual use.
- Build 39 added regroup suggestions so the shell can propose better organizational moves inside the taxonomy layer.
- Build 40 added archive suggestions for watchers, making stale watcher cleanup more operational.
- Build 41 added a maintenance report so watcher/archive hygiene can be reviewed from one surface.
- Build 42 added maintenance history tracking for cleanup and taxonomy operations.
- Build 43 deepened maintenance visibility further so search, taxonomy, and cleanup flows behave more like an operating system than a demo shell.
- Build 44 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 45 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 46 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 47 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 48 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 49 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 50 continued shell hardening and maintenance instrumentation in the staged intelligence workspace.
- Build 51 expanded saved searches so they store and restore full shell context, including query, task/group/group-state/tag filters, selected watcher, and related shell reload behavior when a saved search is selected.
- Build 52 added saved-search lifecycle controls, specifically rename and delete, backed by `PATCH /api/intel/saved-searches/:id` and `DELETE /api/intel/saved-searches/:id`.
- Build 53 extended saved searches to store and restore signal-layer state too, specifically `signalSourceFilter` and `signalSeverityFilter`.
- Build 54 added persistent saved-search pinning plus pinned quick-access chips and pinned-first ordering in the saved-search dropdown.
- Build 55 added saved-search usage analytics by defaulting saved searches to `usageCount: 0` and `lastUsedAt: null`, tracking usage on selection, and exposing `/api/intel/saved-search-analytics`.
- Build 56 added saved-search health reporting through `/api/intel/saved-search-health`, flagging stale and low-value saved searches in the shell.
- Build 57 added repair actions to saved-search health, using one-click pinning as the first safe repair path for low-value saved searches.
- Build 58 added query analytics through `/api/intel/query-analytics`, exposing ranked search demand from tracked search usage and rendering top query chips in the shell.
- Build 59 added a compact search maintenance summary through `/api/intel/search-maintenance-summary`, summarizing tracked queries, total query uses, saved-search counts, pinned counts, and stale counts.
- Build 60 added saved-search cleanup suggestions plus saved-search `archived` state, including archive suggestions for pinned-but-unused searches and delete suggestions for unpinned unused searches via `/api/intel/saved-search-suggestions`.
- Build 61 added query topic clustering through `/api/intel/query-topics`, grouping tracked query demand into higher-level topics such as `Air Watch` and rendering those topics as a new shell surface.
- Local verification continued through Build 61 using `PORT=8899`, and the staged Intelligence work remained inside `/Users/jsg/.openclaw/workspace/jerryknows/` rather than a separate `jongrayson.com` repo.
- Build 62 added query trend labels through `/api/intel/query-trends`, classifying demand as rising, steady, or cooling from count plus recency.
- Build 63 added query-topic drilldown through `/api/intel/query-topics/:topic`, so topic clusters can open into their underlying search terms.
- Build 64 added topic trend labels through `/api/intel/query-topic-trends`, extending movement analysis from raw terms to topic clusters.
- Build 65 added topic-to-saved-search recommendations through `/api/intel/saved-search-recommendations`, letting the shell propose new saved searches from search demand.
- Build 66 added recommendation acceptance analytics through `POST /api/intel/saved-search-recommendations/record-accept` and `GET /api/intel/recommendation-analytics`.
- Build 67 added recommendation quality scoring through `/api/intel/recommendation-quality`, ranking recommendations by demand, acceptance, and whether they were already materialized.
- Build 68 added recommendation auto-prioritization through `/api/intel/recommendation-priority`, and a verification-time syntax error in the sort expression was fixed and logged to `.learnings/ERRORS.md`.
- Build 69 added priority-aware recommendation actions so high-priority recommendations can create saved searches immediately while weaker ones fall back to review/inspect flows.
- Build 70 added recommendation decline tracking through `POST /api/intel/saved-search-recommendations/record-decline` and `GET /api/intel/recommendation-decline-analytics`.
- Build 71 added recommendation net score through `/api/intel/recommendation-net-score`, combining accepts and declines into a positive, neutral, or negative recommendation outcome signal.
- By the end of Build 71, the search and recommendation layer now covers analytics, topics, trend detection, saved-search recommendations, acceptance/decline tracking, quality scoring, priority ranking, priority-aware actions, and net performance scoring.
- Build 89 added escalation recommendation effectiveness via `GET /api/intel/escalation-recommendation-effectiveness`, combining recommendation acceptance, escalation usage, and downstream net outcome into `effectivenessScore` plus `effective`/`mixed`/`weak` labels.
- Build 90 added an escalation recommendation dashboard via `GET /api/intel/escalation-recommendation-dashboard`, summarizing recommendation count, total accepts, and effective/mixed/weak counts.
- Build 91 added escalation recommendation escalations via `GET /api/intel/escalation-recommendation-escalations`, generating stronger follow-up actions such as `escalate-escalation-review` and `escalate-escalation-rework` for weak or mixed recommendation cases.
- Build 92 added second-order escalation analytics with `POST /api/intel/escalation-recommendation-escalations/record-use` and `GET /api/intel/escalation-recommendation-escalation-analytics`; clicking Review on a second-order escalation now records usage, reruns search, and refreshes the shell.
- Build 93 added second-order escalation effectiveness via `GET /api/intel/escalation-recommendation-escalation-effectiveness`, combining second-order escalation count, first-order recommendation acceptance, and downstream net outcome into `effectivenessScore` plus `effective`/`mixed`/`weak` labels.
- Build 94 added a second-order escalation dashboard via `GET /api/intel/escalation-recommendation-escalation-dashboard`, summarizing escalation case count, total escalations, and effective/mixed/weak counts.
- Build 95 added third-order escalation recommendations via `GET /api/intel/third-order-escalation-recommendations`, generating deeper follow-up actions like `escalate-third-order-review` and `escalate-third-order-rework` for weak or mixed second-order cases.
- Local verification through Builds 89-95 continued on `PORT=8899`, with `/intelligence/` showing the matching build labels and new left-rail blocks for each analytics or recommendation surface.
- Build 114 added Archived Feed Restoration Metrics to `jongrayson/intel/`, exposing visibility into recovered signals.
- Build 115 added Signal Recovery Filtering, clicking recovery metrics filters feed to recovered-only.
- Build 116 added Escalation-to-Task Conversion, allowing operators to instantly generate a new intelligence mission from a triaged signal.
- Build 117 added Task Conversion Analytics, showing exactly how many Watchers originated from the live feed.
- Build 118 added a Live Timeline panel below the main stage, rendering physical time markers for active signals.
- Build 119 added Timeline Auto-Scaling, dynamically bounding the timeline axis based on the filtered view.
- Build 120 added Interactive Timeline Scrubbing, clicking a timeline marker immediately filters the feed to signals within that localized temporal window.
