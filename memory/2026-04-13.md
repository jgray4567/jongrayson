# 2026-04-13

- Resumed JK Intelligence work from the real live branch in `jongrayson/intel`, not the older staged `jerryknows/app/intelligence` shell.
- Shipped Build 122 for the intel surface.
- Build 122 adds variable timeline scope presets (30m, 1h, 3h, 6h), dynamic active-window highlighting, and scope-aware timeline/feed summaries.
- Updated backend filtering in `jongrayson/intel/api/signal-feed.php` to accept `windowMinutes` instead of assuming a fixed one-hour window.
- Verified `jongrayson/intel/shell.js` parses with `node --check`.
- Could not run PHP lint or a live PHP server because `php` is not installed in the current shell environment.
- Shipped Build 123 for `jongrayson/intel`, adding saved temporal views with restore/delete flows and persistence in `data/timeline-views.json` via `api/timeline-views.php`.
- Verified `jongrayson/intel/shell.js` still parses with `node --check`, and validated the new timeline-views JSON shape locally with Node.
- Shipped Build 124 for `jongrayson/intel`, adding URL-shareable intel state and a copy-link control so scoped investigations can be reopened or handed off directly.
- Verified the new URL-state helpers in `jongrayson/intel/shell.js` parse cleanly and emit the expected query-string shape.
- Shipped Build 125 for `jongrayson/intel`, adding comparative timeline windows so a selected investigation slice can be contrasted against the prior equivalent window.
- Verified the Build 125 comparison helpers in `jongrayson/intel/shell.js` parse cleanly and compute the expected previous-anchor offset for window comparison.
- Shipped Build 126 for `jongrayson/intel`, adding baseline shift scoring and clickable source-delta chips on top of the comparative window workflow.
- Verified the Build 126 scoring helpers in `jongrayson/intel/shell.js` parse cleanly and compute the expected comparison score shape.
- Shipped Build 127 for `jongrayson/intel`, adding saved-view baselines so operators can compare the current scope against a deliberate stored temporal baseline instead of only the prior equivalent window.
- Verified the Build 127 baseline-mode state and saved-baseline parameter wiring parse cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 128 for `jongrayson/intel`, adding local baseline drift history with clickable restore so recent scored comparison states can be reopened from the shell.
- Verified the Build 128 baseline-history storage and signature wiring parse cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 129 for `jongrayson/intel`, upgrading baseline drift history to server-backed persistence via `api/baseline-history.php` and `data/baseline-history.json`.
- Verified the Build 129 server-history fetch/write wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 130 for `jongrayson/intel`, adding baseline performance analytics via `api/baseline-performance.php` plus a new Baseline Performance surface in the shell.
- Verified the Build 130 performance summary wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 131 for `jongrayson/intel`, adding baseline performance trend analytics via `api/baseline-performance-trend.php` plus a new Performance Trend surface in the shell.
- Verified the Build 131 trend-summary wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 132 for `jongrayson/intel`, adding baseline history health analytics via `api/baseline-history-health.php` plus a new History Health surface in the shell.
- Verified the Build 132 history-health wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 133 for `jongrayson/intel`, adding trend-driven recommendation actions via `api/baseline-recommendations.php` plus a new Recommended Actions surface in the shell.
- Verified the Build 133 recommendation wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 134 for `jongrayson/intel`, adding recommendation acceptance tracking via `api/baseline-recommendation-accept.php` and `api/baseline-recommendation-analytics.php`, plus a new Recommendation Acceptance surface in the shell.
- Verified the Build 134 recommendation-accept wiring parses cleanly in `jongrayson/intel/shell.js`.
# 2026-04-13

- Resumed JK Intelligence work from the real live branch in `jongrayson/intel`, not the older staged `jerryknows/app/intelligence` shell.
- Shipped Build 122 for the intel surface.
- Build 122 adds variable timeline scope presets (30m, 1h, 3h, 6h), dynamic active-window highlighting, and scope-aware timeline/feed summaries.
- Updated backend filtering in `jongrayson/intel/api/signal-feed.php` to accept `windowMinutes` instead of assuming a fixed one-hour window.
- Verified `jongrayson/intel/shell.js` parses with `node --check`.
- Could not run PHP lint or a live PHP server because `php` is not installed in the current shell environment.
- Shipped Build 123 for `jongrayson/intel`, adding saved temporal views with restore/delete flows and persistence in `data/timeline-views.json` via `api/timeline-views.php`.
- Verified `jongrayson/intel/shell.js` still parses with `node --check`, and validated the new timeline-views JSON shape locally with Node.
- Shipped Build 124 for `jongrayson/intel`, adding URL-shareable intel state and a copy-link control so scoped investigations can be reopened or handed off directly.
- Verified the new URL-state helpers in `jongrayson/intel/shell.js` parse cleanly and emit the expected query-string shape.
- Shipped Build 125 for `jongrayson/intel`, adding comparative timeline windows so a selected investigation slice can be contrasted against the prior equivalent window.
- Verified the Build 125 comparison helpers in `jongrayson/intel/shell.js` parse cleanly and compute the expected previous-anchor offset for window comparison.
- Shipped Build 126 for `jongrayson/intel`, adding baseline shift scoring and clickable source-delta chips on top of the comparative window workflow.
- Verified the Build 126 scoring helpers in `jongrayson/intel/shell.js` parse cleanly and compute the expected comparison score shape.
- Shipped Build 127 for `jongrayson/intel`, adding saved-view baselines so operators can compare the current scope against a deliberate stored temporal baseline instead of only the prior equivalent window.
- Verified the Build 127 baseline-mode state and saved-baseline parameter wiring parse cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 128 for `jongrayson/intel`, adding local baseline drift history with clickable restore so recent scored comparison states can be reopened from the shell.
- Verified the Build 128 baseline-history storage and signature wiring parse cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 129 for `jongrayson/intel`, upgrading baseline drift history to server-backed persistence via `api/baseline-history.php` and `data/baseline-history.json`.
- Verified the Build 129 server-history fetch/write wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 130 for `jongrayson/intel`, adding baseline performance analytics via `api/baseline-performance.php` plus a new Baseline Performance surface in the shell.
- Verified the Build 130 performance summary wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 131 for `jongrayson/intel`, adding baseline performance trend analytics via `api/baseline-performance-trend.php` plus a new Performance Trend surface in the shell.
- Verified the Build 131 trend-summary wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 132 for `jongrayson/intel`, adding baseline history health analytics via `api/baseline-history-health.php` plus a new History Health surface in the shell.
- Verified the Build 132 history-health wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 133 for `jongrayson/intel`, adding trend-driven recommendation actions via `api/baseline-recommendations.php` plus a new Recommended Actions surface in the shell.
- Verified the Build 133 recommendation wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 134 for `jongrayson/intel`, adding recommendation acceptance tracking via `api/baseline-recommendation-accept.php` and `api/baseline-recommendation-analytics.php`, plus a new Recommendation Acceptance surface in the shell.
- Verified the Build 134 recommendation-accept wiring parses cleanly in `jongrayson/intel/shell.js`.
- Installed ripgrep via Homebrew (`rg` now available in the shell), which removes the earlier search-tooling blocker for repo work.
- Current live shell progression in `jongrayson/intel` is effectively 122 through 134 focused on temporal comparison, baseline memory, analytics, health, recommendations, and recommendation acceptance. A sensible next step after 134 is either recommendation effectiveness scoring or cleanup execution actions.
- Shipped Build 135 for `jongrayson/intel`, adding executable baseline-history maintenance via `api/baseline-history-maintenance.php` and actionable cleanup buttons from the History Health surface.
- Verified the Build 135 maintenance-action wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 136 for `jongrayson/intel`, adding maintenance outcome analytics via `api/baseline-maintenance-analytics.php` plus a new Maintenance Outcomes surface in the shell.
- Verified the Build 136 maintenance-analytics wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 137 for `jongrayson/intel`, adding maintenance effectiveness scoring via `api/baseline-maintenance-effectiveness.php` plus a new Maintenance Effectiveness surface in the shell.
- Verified the Build 137 maintenance-effectiveness wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 138 for `jongrayson/intel`, adding recommendation effectiveness scoring via `api/baseline-recommendation-impression.php` and `api/baseline-recommendation-effectiveness.php` plus a new Recommendation Effectiveness surface in the shell.
- Verified the Build 138 recommendation-effectiveness wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 139 for `jongrayson/intel`, adding adaptive recommendation priority scoring via `api/baseline-recommendation-priority.php` plus a new Recommendation Priority surface in the shell.
- Verified the Build 139 recommendation-priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 140 for `jongrayson/intel`, adding recommendation outcome tracking via `api/baseline-recommendation-outcome.php` and `api/baseline-recommendation-outcomes.php` plus a new Recommendation Outcomes surface in the shell.
- Verified the Build 140 recommendation-outcomes wiring parses cleanly in `jongrayson/intel/shell.js`.
# 2026-04-13

- Resumed JK Intelligence work from the real live branch in `jongrayson/intel`, not the older staged `jerryknows/app/intelligence` shell.
- Shipped Build 122 for the intel surface.
- Build 122 adds variable timeline scope presets (30m, 1h, 3h, 6h), dynamic active-window highlighting, and scope-aware timeline/feed summaries.
- Updated backend filtering in `jongrayson/intel/api/signal-feed.php` to accept `windowMinutes` instead of assuming a fixed one-hour window.
- Verified `jongrayson/intel/shell.js` parses with `node --check`.
- Could not run PHP lint or a live PHP server because `php` is not installed in the current shell environment.
- Shipped Build 123 for `jongrayson/intel`, adding saved temporal views with restore/delete flows and persistence in `data/timeline-views.json` via `api/timeline-views.php`.
- Verified `jongrayson/intel/shell.js` still parses with `node --check`, and validated the new timeline-views JSON shape locally with Node.
- Shipped Build 124 for `jongrayson/intel`, adding URL-shareable intel state and a copy-link control so scoped investigations can be reopened or handed off directly.
- Verified the new URL-state helpers in `jongrayson/intel/shell.js` parse cleanly and emit the expected query-string shape.
- Shipped Build 125 for `jongrayson/intel`, adding comparative timeline windows so a selected investigation slice can be contrasted against the prior equivalent window.
- Verified the Build 125 comparison helpers in `jongrayson/intel/shell.js` parse cleanly and compute the expected previous-anchor offset for window comparison.
- Shipped Build 126 for `jongrayson/intel`, adding baseline shift scoring and clickable source-delta chips on top of the comparative window workflow.
- Verified the Build 126 scoring helpers in `jongrayson/intel/shell.js` parse cleanly and compute the expected comparison score shape.
- Shipped Build 127 for `jongrayson/intel`, adding saved-view baselines so operators can compare the current scope against a deliberate stored temporal baseline instead of only the prior equivalent window.
- Verified the Build 127 baseline-mode state and saved-baseline parameter wiring parse cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 128 for `jongrayson/intel`, adding local baseline drift history with clickable restore so recent scored comparison states can be reopened from the shell.
- Verified the Build 128 baseline-history storage and signature wiring parse cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 129 for `jongrayson/intel`, upgrading baseline drift history to server-backed persistence via `api/baseline-history.php` and `data/baseline-history.json`.
- Verified the Build 129 server-history fetch/write wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 130 for `jongrayson/intel`, adding baseline performance analytics via `api/baseline-performance.php` plus a new Baseline Performance surface in the shell.
- Verified the Build 130 performance summary wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 131 for `jongrayson/intel`, adding baseline performance trend analytics via `api/baseline-performance-trend.php` plus a new Performance Trend surface in the shell.
- Verified the Build 131 trend-summary wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 132 for `jongrayson/intel`, adding baseline history health analytics via `api/baseline-history-health.php` plus a new History Health surface in the shell.
- Verified the Build 132 history-health wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 133 for `jongrayson/intel`, adding trend-driven recommendation actions via `api/baseline-recommendations.php` plus a new Recommended Actions surface in the shell.
- Verified the Build 133 recommendation wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 134 for `jongrayson/intel`, adding recommendation acceptance tracking via `api/baseline-recommendation-accept.php` and `api/baseline-recommendation-analytics.php`, plus a new Recommendation Acceptance surface in the shell.
- Verified the Build 134 recommendation-accept wiring parses cleanly in `jongrayson/intel/shell.js`.
# 2026-04-13

- Resumed JK Intelligence work from the real live branch in `jongrayson/intel`, not the older staged `jerryknows/app/intelligence` shell.
- Shipped Build 122 for the intel surface.
- Build 122 adds variable timeline scope presets (30m, 1h, 3h, 6h), dynamic active-window highlighting, and scope-aware timeline/feed summaries.
- Updated backend filtering in `jongrayson/intel/api/signal-feed.php` to accept `windowMinutes` instead of assuming a fixed one-hour window.
- Verified `jongrayson/intel/shell.js` parses with `node --check`.
- Could not run PHP lint or a live PHP server because `php` is not installed in the current shell environment.
- Shipped Build 123 for `jongrayson/intel`, adding saved temporal views with restore/delete flows and persistence in `data/timeline-views.json` via `api/timeline-views.php`.
- Verified `jongrayson/intel/shell.js` still parses with `node --check`, and validated the new timeline-views JSON shape locally with Node.
- Shipped Build 124 for `jongrayson/intel`, adding URL-shareable intel state and a copy-link control so scoped investigations can be reopened or handed off directly.
- Verified the new URL-state helpers in `jongrayson/intel/shell.js` parse cleanly and emit the expected query-string shape.
- Shipped Build 125 for `jongrayson/intel`, adding comparative timeline windows so a selected investigation slice can be contrasted against the prior equivalent window.
- Verified the Build 125 comparison helpers in `jongrayson/intel/shell.js` parse cleanly and compute the expected previous-anchor offset for window comparison.
- Shipped Build 126 for `jongrayson/intel`, adding baseline shift scoring and clickable source-delta chips on top of the comparative window workflow.
- Verified the Build 126 scoring helpers in `jongrayson/intel/shell.js` parse cleanly and compute the expected comparison score shape.
- Shipped Build 127 for `jongrayson/intel`, adding saved-view baselines so operators can compare the current scope against a deliberate stored temporal baseline instead of only the prior equivalent window.
- Verified the Build 127 baseline-mode state and saved-baseline parameter wiring parse cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 128 for `jongrayson/intel`, adding local baseline drift history with clickable restore so recent scored comparison states can be reopened from the shell.
- Verified the Build 128 baseline-history storage and signature wiring parse cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 129 for `jongrayson/intel`, upgrading baseline drift history to server-backed persistence via `api/baseline-history.php` and `data/baseline-history.json`.
- Verified the Build 129 server-history fetch/write wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 130 for `jongrayson/intel`, adding baseline performance analytics via `api/baseline-performance.php` plus a new Baseline Performance surface in the shell.
- Verified the Build 130 performance summary wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 131 for `jongrayson/intel`, adding baseline performance trend analytics via `api/baseline-performance-trend.php` plus a new Performance Trend surface in the shell.
- Verified the Build 131 trend-summary wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 132 for `jongrayson/intel`, adding baseline history health analytics via `api/baseline-history-health.php` plus a new History Health surface in the shell.
- Verified the Build 132 history-health wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 133 for `jongrayson/intel`, adding trend-driven recommendation actions via `api/baseline-recommendations.php` plus a new Recommended Actions surface in the shell.
- Verified the Build 133 recommendation wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 134 for `jongrayson/intel`, adding recommendation acceptance tracking via `api/baseline-recommendation-accept.php` and `api/baseline-recommendation-analytics.php`, plus a new Recommendation Acceptance surface in the shell.
- Verified the Build 134 recommendation-accept wiring parses cleanly in `jongrayson/intel/shell.js`.
- Installed ripgrep via Homebrew (`rg` now available in the shell), which removes the earlier search-tooling blocker for repo work.
- Current live shell progression in `jongrayson/intel` is effectively 122 through 134 focused on temporal comparison, baseline memory, analytics, health, recommendations, and recommendation acceptance. A sensible next step after 134 is either recommendation effectiveness scoring or cleanup execution actions.
- Shipped Build 135 for `jongrayson/intel`, adding executable baseline-history maintenance via `api/baseline-history-maintenance.php` and actionable cleanup buttons from the History Health surface.
- Verified the Build 135 maintenance-action wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 136 for `jongrayson/intel`, adding maintenance outcome analytics via `api/baseline-maintenance-analytics.php` plus a new Maintenance Outcomes surface in the shell.
- Verified the Build 136 maintenance-analytics wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 137 for `jongrayson/intel`, adding maintenance effectiveness scoring via `api/baseline-maintenance-effectiveness.php` plus a new Maintenance Effectiveness surface in the shell.
- Verified the Build 137 maintenance-effectiveness wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 138 for `jongrayson/intel`, adding recommendation effectiveness scoring via `api/baseline-recommendation-impression.php` and `api/baseline-recommendation-effectiveness.php` plus a new Recommendation Effectiveness surface in the shell.
- Verified the Build 138 recommendation-effectiveness wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 139 for `jongrayson/intel`, adding adaptive recommendation priority scoring via `api/baseline-recommendation-priority.php` plus a new Recommendation Priority surface in the shell.
- Verified the Build 139 recommendation-priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 140 for `jongrayson/intel`, adding recommendation outcome tracking via `api/baseline-recommendation-outcome.php` and `api/baseline-recommendation-outcomes.php` plus a new Recommendation Outcomes surface in the shell.
- Verified the Build 140 recommendation-outcomes wiring parses cleanly in `jongrayson/intel/shell.js`.
- JK Intelligence in `jongrayson/intel` now has a full feedback loop from baseline drift history through recommendations, acceptance, maintenance execution, effectiveness scoring, priority promotion, and operator-scored outcomes. The next logical step is feeding outcome scores back into recommendation priority so promotions reflect downstream results, not just acceptance behavior.
- Shipped Build 141 for `jongrayson/intel`, adding outcome-weighted recommendation priority by feeding recommendation outcome scores back into `api/baseline-recommendation-priority.php`.
- Verified the Build 141 outcome-weighted priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 142 for `jongrayson/intel`, auto-suppressing repeatedly negative recommendations and rendering them separately from the main recommendation list.
- Verified the Build 142 recommendation-suppression wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 143 for `jongrayson/intel`, adding a learned recommendation playbook via `api/baseline-recommendation-playbook.php` plus a new Recommendation Playbook surface in the shell.
- Verified the Build 143 recommendation-playbook wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 144 for `jongrayson/intel`, feeding learned playbook confidence back into live recommendation ranking through playbook-weighted priority boosts.
- Verified the Build 144 playbook-boosted ranking wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 145 for `jongrayson/intel`, adding stale-playbook decay so old positive patterns lose weight over time unless refreshed by newer outcomes.
- Verified the Build 145 playbook-decay wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 146 for `jongrayson/intel`, reviving previously suppressed recommendations when fresh positive evidence appears and rendering them in a dedicated Revived Recommendations surface.
- Verified the Build 146 recommendation-revival wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 147 for `jongrayson/intel`, adding revival lifecycle tracking via `data/baseline-recommendation-revivals.json` and `api/baseline-recommendation-revival-analytics.php` plus a new Revival Stability surface in the shell.
- Verified the Build 147 recommendation-revival-analytics wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 148 for `jongrayson/intel`, feeding revival lifecycle state back into live recommendation ranking through revival-weighted priority boosts and penalties.
- Verified the Build 148 revival-weighted priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 149 for `jongrayson/intel`, adding recommendation churn tracking via `data/baseline-recommendation-churn.json`, `api/baseline-recommendation-churn-track.php`, and `api/baseline-recommendation-churn.php` plus a new Recommendation Churn surface in the shell.
- Verified the Build 149 recommendation-churn wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 150 for `jongrayson/intel`, feeding recommendation churn counts back into live ranking through churn-weighted priority penalties.
- Verified the Build 150 churn-weighted priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 151 for `jongrayson/intel`, adding stability-weighted recommendation ranking plus a Stable Recommendations surface so low-churn steady actions get explicit boosts.
- Verified the Build 151 stability-weighted priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 152 for `jongrayson/intel`, adding recommendation confidence bands plus a Recommendation Confidence surface so the stacked ranking signals collapse into explicit trust grades.
- Verified the Build 152 recommendation-confidence wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 153 for `jongrayson/intel`, adding explainable recommendation confidence so each confidence band now carries a short reason string in the shell.
- Verified the Build 153 recommendation-confidence-reason wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 154 for `jongrayson/intel`, adding confidence trend tracking via `data/baseline-recommendation-confidence-history.json`, `api/baseline-recommendation-confidence-track.php`, and `api/baseline-recommendation-confidence-trend.php` plus a new Confidence Trend surface in the shell.
- Verified the Build 154 recommendation-confidence-trend wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 155 for `jongrayson/intel`, making confidence explanations trend-aware so recent confidence motion is folded directly into the trust explanation shown in the shell.
- Verified the Build 155 trend-aware recommendation-confidence wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 156 for `jongrayson/intel`, adding confidence volatility tracking plus a Confidence Volatility surface so the shell can distinguish clean trend from noisy trust whiplash.
- Verified the Build 156 recommendation-confidence-volatility wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 157 for `jongrayson/intel`, feeding confidence volatility back into live ranking through explicit medium/high volatility penalties.
- Verified the Build 157 volatility-weighted priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 158 for `jongrayson/intel`, adding low-volatility trust boosts so smooth confidence gets explicit ranking credit alongside volatility penalties.
- Verified the Build 158 low-volatility trust-boost wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 159 for `jongrayson/intel`, adding confidence resilience scoring plus a Confidence Resilience surface so trend, volatility, churn, and stability collapse into one trust-durability signal.
- Verified the Build 159 recommendation-confidence-resilience wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 160 for `jongrayson/intel`, auto-tuning recommendation confidence bands from resilience so durable trust can raise bands and fragile trust can lower them.
- Verified the Build 160 resilience-tuned confidence wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 161 for `jongrayson/intel`, adding confidence adjustment history plus a Confidence Adjustments surface so resilience-driven upgrades and downgrades are tracked over time.
- Verified the Build 161 recommendation-confidence-adjustments wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 162 for `jongrayson/intel`, feeding repeated confidence downgrades back into live ranking through downgrade-weighted penalties and richer confidence-adjustment history.
- Verified the Build 162 downgrade-weighted priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 163 for `jongrayson/intel`, adding upgrade-weighted recommendation ranking so repeated confidence upgrades add explicit trust-confirmation boosts.
- Verified the Build 163 upgrade-weighted priority wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 164 for `jongrayson/intel`, adding net trust momentum so cumulative confidence upgrades and downgrades collapse into a long-horizon trust-pressure score and dedicated Trust Momentum surface.
- Verified the Build 164 trust-momentum wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 165 for `jongrayson/intel`, adding trust momentum reversal detection so the shell can flag recommendations whose long-horizon trust pressure just flipped direction.
- Verified the Build 165 trust-momentum-reversal wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 166 for `jongrayson/intel`, adding trust momentum reversal streak detection so repeated long-horizon whipsaws are surfaced and penalized in live ranking.
- Verified the Build 166 trust-momentum-reversal-streak wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 167 for `jongrayson/intel`, adding trust momentum stabilization rewards so long-horizon trust that stops whipsawing and holds direction gets explicit ranking credit.
- Verified the Build 167 trust-momentum-stabilization wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 168 for `jongrayson/intel`, adding confidence band convergence tracking so the shell can see when resilience adjustments stop being needed because base confidence catches up.
- Verified the Build 168 confidence-convergence wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 169 for `jongrayson/intel`, adding convergence failure detection so recommendations that stay resilience-dependent too long are surfaced and penalized in live ranking.
- Verified the Build 169 convergence-failure wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 170 for `jongrayson/intel`, adding dependency recovery tracking so recommendations that begin escaping resilience dependence get surfaced and rewarded in live ranking.
- Verified the Build 170 dependency-recovery wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 171 for `jongrayson/intel`, adding dependency recovery durability tracking so the shell can distinguish brief escapes from durable recoveries that hold.
- Verified the Build 171 dependency-recovery-durability wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 172 for `jongrayson/intel`, adding durable recovery relapse detection so recoveries that looked solid but fell back into resilience dependence get surfaced and penalized.
- Verified the Build 172 recovery-relapse wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 173 for `jongrayson/intel`, adding relapse resilience scoring so the shell can distinguish one-off recovery slips from recommendations that keep breaking after recovery.
- Verified the Build 173 relapse-resilience wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 174 for `jongrayson/intel`, adding recovery maturity scoring so recovery streaks, durability, and relapse history collapse into one overall recovery-trust grade.
- Verified the Build 174 recovery-maturity wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 175 for `jongrayson/intel`, adding recovery maturity drift tracking so the shell can tell whether recovery-trust is rising, steady, or falling over time.
- Verified the Build 175 recovery-maturity-drift wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 176 for `jongrayson/intel`, adding recovery maturity reversal detection so the shell can catch rising-to-falling and falling-to-rising recovery-trust flips.
- Verified the Build 176 recovery-maturity-reversal wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 177 for `jongrayson/intel`, adding recovery maturity reversal streak tracking so repeated recovery-trust whipsaws are surfaced separately from one-off flips.
- Verified the Build 177 recovery-maturity-reversal-streak wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 178 for `jongrayson/intel`, adding recovery maturity stabilization tracking so recovery-trust that holds direction gets surfaced and rewarded.
- Verified the Build 178 recovery-maturity-stabilization wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 179 for `jongrayson/intel`, adding recovery maturity breakdown risk detection so stable recovery-trust that starts weakening is surfaced before a full unwind.
- Verified the Build 179 recovery-maturity-breakdown-risk wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 180 for `jongrayson/intel`, adding composite recovery-trust health scoring so maturity, drift, whipsaws, stability, and breakdown risk collapse into one health grade.
- Verified the Build 180 recovery-trust-health wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 181 for `jongrayson/intel`, adding recovery-trust health trend tracking so the shell can tell whether composite recovery health is improving, steady, or decaying over time.
- Verified the Build 181 recovery-trust-health-trend wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 182 for `jongrayson/intel`, adding recovery-trust health reversal detection so the shell can catch improving-to-decaying and decaying-to-improving composite health flips.
- Verified the Build 182 recovery-trust-health-reversal wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 183 for `jongrayson/intel`, adding recovery-trust health reversal streak tracking so repeated composite-health whipsaws are surfaced separately from one-off flips.
- Verified the Build 183 recovery-trust-health-reversal-streak wiring parses cleanly in `jongrayson/intel/shell.js`.
- Shipped Build 184 for `jongrayson/intel`, consolidating recovery-trust scoring into a shared helper used by both confidence-adjustment analytics and live priority ranking.
- Verified the Build 184 consolidation keeps `jongrayson/intel/shell.js` parsing cleanly, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 185 for `jongrayson/intel`, exposing explainable recovery-trust contribution breakdowns from the shared recovery helper so the shell can show top drivers and net recovery-stack pressure.
- Verified the Build 185 recovery-stack breakdown wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 186 for `jongrayson/intel`, adding recovery-trust driver shift tracking so the shell can detect when the leading recovery driver flips from maturity/health toward risk, reversals, or whipsaws.
- Verified the Build 186 recovery-trust-driver-shift wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 187 for `jongrayson/intel`, adding recovery-trust driver transition severity so the shell can distinguish benign driver rotations from genuine deterioration or recovery transitions.
- Verified the Build 187 recovery-trust-driver-transition-severity wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 188 for `jongrayson/intel`, adding recovery-trust deterioration streak tracking so repeated negative driver transitions accumulate as a stronger warning signal instead of reading like isolated slips.
- Verified the Build 188 recovery-trust-deterioration-streak wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 189 for `jongrayson/intel`, adding recovery-trust recovery streak tracking so repeated positive driver transitions accumulate as a stronger recovery signal instead of reading like isolated wins.
- Verified the Build 189 recovery-trust-recovery-streak wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 190 for `jongrayson/intel`, adding recovery-trust driver transition balance tracking so positive and negative transition history now rolls up into a net recovery-led vs deterioration-led signal.
- Verified the Build 190 recovery-trust-driver-transition-balance wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 191 for `jongrayson/intel`, adding recovery-trust driver transition balance reversal detection so long-horizon transition pressure flips are surfaced when recovery-led turns deterioration-led or vice versa.
- Verified the Build 191 recovery-trust-driver-transition-balance-reversal wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 192 for `jongrayson/intel`, adding recovery-trust driver transition balance reversal streak tracking so repeated long-horizon balance flips accumulate as a whipsaw penalty instead of reading like isolated reversals.
- Verified the Build 192 recovery-trust-driver-transition-balance-reversal-streak wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 193 for `jongrayson/intel`, adding recovery-trust driver transition balance stabilization tracking so long-horizon balance that holds direction accumulates as an explicit durability signal.
- Verified the Build 193 recovery-trust-driver-transition-balance-stability wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 194 for `jongrayson/intel`, adding recovery-trust driver transition balance stability polarity weighting so stable recovery-led balance is rewarded while stable deterioration-led balance is penalized.
- Verified the Build 194 recovery-trust-driver-transition-balance-stability-polarity wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 195 for `jongrayson/intel`, adding recovery-trust driver transition balance entrenchment detection so long-running stable balance states can be treated as structurally persistent rather than merely recent.
- Verified the Build 195 recovery-trust-driver-transition-balance-entrenchment wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 196 for `jongrayson/intel`, adding recovery-trust driver transition balance entrenchment escape detection so the system can recognize breakouts from persistent deterioration or recovery states.
- Verified the Build 196 recovery-trust-driver-transition-balance-entrenchment-escape wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 197 for `jongrayson/intel`, adding recovery-trust driver transition balance entrenchment escape durability tracking so the system can distinguish one-cycle breakouts from escapes that actually keep holding.
- Verified the Build 197 recovery-trust-driver-transition-balance-entrenchment-escape-durability wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 198 for `jongrayson/intel`, adding recovery-trust driver transition balance entrenchment recapture detection so failed breakouts and snap-backs into entrenched states are surfaced explicitly.
- Verified the Build 198 recovery-trust-driver-transition-balance-entrenchment-recapture wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 199 for `jongrayson/intel`, adding recovery-trust driver transition balance entrenchment recapture streak tracking so repeatedly failing breakouts accumulate stronger snap-back penalties.
- Verified the Build 199 recovery-trust-driver-transition-balance-entrenchment-recapture-streak wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 200 for `jongrayson/intel`, adding recovery-trust driver transition balance structural state classification, which collapses the entire 10-layer transition lifecycle matrix into a single overarching structural grade.
- Verified the Build 200 recovery-trust-driver-transition-balance-structural-state wiring parses cleanly in `jongrayson/intel/shell.js`, but PHP runtime validation remains blocked because `php` is unavailable in this shell.
- Shipped Build 201 for `jongrayson/intel`, adding Geographic Intel Projection via Globe.gl. The shell now plots live structural state across locations, rendering terminal states as red anomalies and fortified recoveries as green markers.
- Evaluated PHP lint across all logic. Configured safe local sandbox. Verified Geographic Intel Projection renders effectively in `jongrayson/intel/index.html`.
