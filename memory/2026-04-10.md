# 2026-04-10

## JerryKnows Web Team MVP — Completed Tonight

Full end-to-end pipeline is working locally. Verified with a real push to `exita1a-landing` on GitHub.

### What's done
- Phase 1-8 complete (foundation → UI → inspect → plan → edit → approve → commit → push → polish)
- `src/inspector.js` — auto-detects static site structure on job creation
- `src/editor.js` — safe file patching with path traversal protection
- `src/telegram.js` — dedicated Telegram module with long-poll support, inline buttons, text fallback
- Gemini 2.5 Flash via `profiles.js` jerry profile for plan generation
- Auto-rebase on push rejection (pull --rebase + retry)
- Job list: newest-first, status emojis, commit SHA
- Job detail: Release card, Inspection card, History
- Known repos hardcoded in site selector: `exita1a-landing`, `a1asells`
- MVP success criteria: 6/6 ✅

### InspectAI — Compliance Automation Platform (New Project)
- Kevin (Jon's friend) is a DHP inspector in Virginia with a backlog in the hundreds
- Concept: Self-service compliance portal for Virginia Dept. of Health Professions (DHP)
- Facilities self-submit compliance info + photos online
- AI validates photos (object recognition, GPS match, EXIF timestamp, tamper detection)
- Inspector reviews remotely — onsite only for first-time, failures, or flagged submissions
- PDF report auto-generated, submitted to DHP API (assumed)
- Users: Client (facility), Inspector (Kevin), Admin
- Compliance: HIPAA, Virginia §2.2-3800, §42.1-76 (10yr retention), NIST 800-53
- Business potential: SaaS product, every state has same backlog problem
- Tech stack: React + React Native + Node.js + PostgreSQL + AWS S3 + GPT-4o Vision
- Build approach: Codex (boilerplate/scaffolding) + Claude Code (complex logic) + Jarvis orchestrates and deploys
- Timeline: ~3 months to MVP
- NEXT STEP: Kevin to provide Excel workbook (current inspection form)
- Google Doc saved: https://docs.google.com/document/d/1cSGyJp81WtyobEoNUBX_2W6d7LJVtARF-98OBOaZN24/edit
- Google Drive folder: InspectAI (ID: 1VA79GBNq0vFPp8gW_mQ6b8dBZ2K7ZklE)

### JerryKnows AI Final Feature Set
- Multi-model via OpenRouter (Gemini 2.5 Flash default, Claude Sonnet, GPT-4o, Llama 3.3 70B, Mistral Large, Gemini 2.5 Pro)
- OpenRouter key stored in VPS .env, pulled from Google Drive doc
- PDF upload with instant extract-and-discard (pdf-parse, multer, 20MB limit, 15k char cap)
- Memory system: auto-extracts facts per user after each chat, injects context on new chats, 🧠 panel to view/delete/clear
- ChatGPT-style UI: collapsible sidebar, model picker dropdown, copy/edit buttons, typing dots
- Light color scheme matching Web Team
- Users: jon, adam, kevin, sara, marybeth, steve / password: JK2026$

### VPS — DONE ✅
- `https://api.jerryknows.ai` — live, HTTPS, nginx reverse proxy
- pm2 managing jk-webteam process, auto-restarts
- @jk_webteam_bot polling for inline button approvals
- SSL via Let's Encrypt, auto-renews

### VPS TODO (morning)
1. Create dedicated JerryKnows Telegram bot via @BotFather → add `JERRYKNOWS_BOT_TOKEN` to VPS `.env`
2. Strip Hilda/billing from VPS profile (per `JK-WEBTEAM-VPS-PROFILE.md`)
3. Deploy to VPS with correct `.env` (Gemini key, dedicated bot token, TELEGRAM_CHAT_ID)
4. Register JerryKnows bot webhook OR confirm long-polling works on VPS
5. Set up process manager (pm2 or systemd) to keep the server running

### Server currently running
- `http://localhost:8787` (shared bot mode, text approval codes only)
- Bot: shared OpenClaw token — inline buttons work for sending, text codes for receiving
- Active job: `wt-20260410-001` — status: pushed ✅

## DataBadger
- Excel file in `/workspace/DataBadger/` — 58 probate records from Prince William County VA, Feb-Mar 2026
- Extracted to `extracted.csv`
- Created Google Sheet: https://docs.google.com/spreadsheets/d/1oeMdFwaY01wqqXIkC-iCNce7DptnFWW-HRrHHMXk8ec/edit
- PWC LRMS subscription: $240/year for bulk access (land records + probate)
- Data brokers buy county FTP feeds + skip trace via LexisNexis/Experian

## DataBadger repo
- New repo created, static HTML site
- Hosting plan: Cloudflare Pages + custom domain `mydatabadger.com`
- Not yet deployed

### VPS Deployment & Web Team Completion
- **VPS Live:** Deployed JK Web Team to the Ubuntu VPS at `api.jerryknows.ai` via nginx and Let's Encrypt SSL.
- **Telegram Bot:** Created a dedicated Telegram bot (`@jk_webteam_bot`) for Web Team approvals, enabling native inline button actions (Approve, Revise, Cancel) without OpenClaw token conflicts.
- **GitHub SSH Auth:** Set up an ed25519 SSH key on the VPS and linked it to Jon's GitHub account, allowing the agent to natively `git push` approved changes. 
- **Web Team UI Revamp:** Stripped out Bullpen and Billing modules completely. Implemented a prompt-first design (a centralized "Describe the change" textarea that expands into a confirmation modal with repo details).
- **Sites Handled:** 
  - `exita1a.com` (`exita1a-landing`)
  - `giuseppesva.com` (`giuseppesva-site`)
  - `pickleopsapp.com` (`pickleops-investor-site`)
  - `mydatabadger.com` (`databadger`)

### BoglAI (Boglehead Financial Advisor)
- Built and launched a dedicated Boglehead philosophy-constrained AI advisor (`bogleai`).
- **Features:** Portfolio Analyzer (fee drag, tax efficiency, Bogle score) and LLM Advisor using RAG over Bogleheads philosophy and Wikipedia financial summaries.
- Running on the VPS on port 8789. Waiting to wire to `bogleai.jerryknows.ai` via Cloudflare/nginx.

### JerryKnows AI (ChatGPT-style Interface)
- Built a standalone ChatGPT-style interface (`jerryknows-ai`) to live on the root `jerryknows.ai` domain.
- **Features:** Multi-turn chat, markdown rendering, code blocks, sidebar history, typing indicators, and **live web search** using the Brave Search API.
- Running on port 8791 on the VPS. Waiting for the Brave API key to be provided via the UI to finalize deployment.
